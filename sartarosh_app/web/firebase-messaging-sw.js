// This file is required by Firebase Messaging for Flutter web.
// Since we handle messages in Dart, this can be largely empty to prevent 404s.
self.addEventListener('push', function (event) {
    // Push listener
});
